document.getElementById('meuFormulario').addEventListener('submit', function (event) {
    event.preventDefault();

    // Aqui você pode adicionar lógica para processar os dados do formulário
    // Por exemplo, você pode acessar os valores dos campos usando document.getElementById
    // e então enviar esses dados para um servidor usando AJAX ou fetch.
    
    alert('Formulário enviado com sucesso!');
});
